discord.py==2.7.1
python-dotenv==1.2.2
aiosqlite==0.22.1
boto3==1.43.88
aiohttp==3.14.3
requests==2.34.2
feedparser==6.0.12
# !dl / "purgito dl" (cogs/download.py): descarga de videos de Instagram.
yt-dlp==2026.8.19
regex==2026.9.3
Pillow==12.3.0
imagehash==4.3.2
groq==1.7.0
aiohttp-session[secure]==2.12.1
# cryptography no es dependencia directa (la trae el extra [secure] de
# aiohttp-session para el Fernet de EncryptedCookieStorage, la cookie de
# sesión cifrada) -- se pinea igual, explícita, porque el resto del archivo
# pinea todo con == y esta toca cifrado de sesión: sin el pin acá, un
# `pip install` nuevo resuelve la versión que sea que el extra permita en
# ese momento en vez de la que se auditó.
cryptography==50.0.1
polar-sdk==0.31.7
edge-tts==7.2.8
PyNaCl==1.6.2
